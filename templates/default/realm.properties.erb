<% unless @rundeck_users.key?('admin') %>
admin:<%= @admin_password %>,user,admin,architect,deploy,build
<% end %>
<% @rundeck_users.each do |key,array| %>
<%= key %>:<%= array[:password] %>,<%= array[:groups].kind_of?(Array) ? array[:groups].join(",") : array[:groups] %>
<% end %>
