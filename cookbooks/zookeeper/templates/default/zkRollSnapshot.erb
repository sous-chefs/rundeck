#!/usr/bin/env bash

COMMAND="java"
if [ "$(whoami)" != "zookeeper" ]
then
	COMMAND="sudo -u zookeeper java"
fi

logger -s "zkRollSnapshot"

<%= node[:zookeeper][:install_dir] %>/current/bin/zkCleanup.sh  <%= node[:zookeeper][:data_dir] %> <%= node[:zookeeper][:snapshot_dir] %> <%= node[:zookeeper][:snapshot_num] %>

exit 0
