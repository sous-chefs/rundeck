### Configuration lines specify one web address to proxy per line;
### each line of the property is of the form:
### easy.proxy.N = web-host -> proxy-host:port
### Example:
# easy.proxy.1 = auth.webtrends.com -> proxy01.foo.com:9090


# Placeholder config file - Edge server supports, but does not currently use, the proxy functionaity.
